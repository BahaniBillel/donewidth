import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CategoryPickerItem = ({label,onPress}) => {
  return (
    <View style={styles.container}>
      
    </View>
  )
}

export default CategoryPickerItem

const styles = StyleSheet.create({
    container:{
        
    }
})